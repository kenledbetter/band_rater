<h1><%= @band.name %></h1>
<p><%= @band.location %></p>
<p><%= link_to @band.url, @band.url %></p>
<p><%= @band.description %></p>
<p>Average rating: <%= number_with_precision(@band.average_rating, :precision => 2) %></p>
<% if current_user %>
  <%= render :partial => 'ratings/form' %>
<% end %>

<br />

<h2>Rating</h2>
<%= render :partial => 'ratings/list' %>
